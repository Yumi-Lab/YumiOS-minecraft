#!/usr/bin/env bash
# shellcheck disable=SC2034

# Heap JVM — SmartPi One 1GB DDR3 (réduit pour laisser place à PufferPanel)
[ -n "${MINECRAFT_XMS}" ]          || MINECRAFT_XMS="512M"
[ -n "${MINECRAFT_XMX}" ]          || MINECRAFT_XMX="700M"

# Dépendances (Java 21 installé explicitement via backports dans start_chroot_script)
[ -n "${MODULE_DEPS}" ]            || MODULE_DEPS="wget curl python3"
